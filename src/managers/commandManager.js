const { BaseCommandManager } = require("@tryforge/forgescript");

class CommandManager extends BaseCommandManager {
    handlerName = "QuorielEdgeEvents";
}

module.exports = { CommandManager };